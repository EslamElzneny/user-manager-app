import { SearchFirebasePipe } from './search-firebase.pipe';

describe('SearchFirebasePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFirebasePipe();
    expect(pipe).toBeTruthy();
  });
});
