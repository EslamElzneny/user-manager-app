import { DateDisplayFirebasePipe } from './date-display-firebase.pipe';

describe('DateDisplayFirebasePipe', () => {
  it('create an instance', () => {
    const pipe = new DateDisplayFirebasePipe();
    expect(pipe).toBeTruthy();
  });
});
